package com.bookconnect.model;

/**
 * Enum representing user roles in the system.
 * 
 * Design Pattern: Enumeration (Type-Safe constant pattern)
 * Used for role-based access control (RBAC).
 */
public enum Role {
    USER,
    ADMIN
}
